name: "HIL: nsim Samples"

on:
  workflow_dispatch:
    inputs:
      api-url:
        required: true
        type: string
      api-key-id:
        required: true
        type: string
      coap_gateway_url:
        required: true
        type: string
      platform:
        required: true
        type: string
      west_board:
        required: true
        type: string

  workflow_call:
    inputs:
      api-url:
        required: true
        type: string
      api-key-id:
        required: true
        type: string
      coap_gateway_url:
        required: true
        type: string
      platform:
        required: true
        type: string
      west_board:
        required: true
        type: string

jobs:
  matrix:
    name: zephyr-${{ inputs.west_board }}-sample-matrix
    uses: ./.github/workflows/misc-locate-twister-samples.yml
    with:
      west_board: ${{ inputs.west_board }}

  build_and_test:
    name: zephyr-${{ inputs.west_board }}-twister (${{ matrix.name }})
    needs:
      - matrix
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.matrix.outputs.sample_list) }}
    runs-on: ubuntu-24.04
    container:
      # yamllint disable-line rule:line-length
      image: golioth/golioth-zephyr-base:ee62f47-sdk-0.17.4@sha256:a8561b8de5a23f9eb28101734d672e0582328add515ce4b4788428cd126e6e2b
    env:
      ZEPHYR_SDK_INSTALL_DIR: /opt/toolchains/zephyr-sdk-0.17.4
      EXTRA_TWISTER_ARGS: ${{ github.event_name == 'pull_request' && '-e nightly' || '' }}
    steps:
      - name: Checkout repository and submodules
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          path: modules/lib/golioth-firmware-sdk
          submodules: 'recursive'

      - name: Init and update west
        run: |
          mkdir -p .west
          cat <<EOF > .west/config
          [manifest]
          path = modules/lib/golioth-firmware-sdk
          file = .ci-west-zephyr.yml
          EOF

          west update -o=--depth=1 -n
          git config --global user.email user@git-scm.com
          git config --global user.name "Git User"

          uv pip install \
            -r zephyr/scripts/requirements-base.txt \
            -r zephyr/scripts/requirements-build-test.txt \
            -r zephyr/scripts/requirements-run-test.txt \
            modules/lib/golioth-firmware-sdk/tests/hil/scripts/pytest-zephyr-samples \
            git+https://github.com/golioth/python-golioth-tools@v0.8.1 \
            asyncclick==8.1.8

      - name: Run tests
        run: |
          rm -rf allure-reports
          zephyr/scripts/twister                                                    \
              -v                                                                    \
              --platform ${{ inputs.west_board }}                                   \
              -T ${{ matrix.dir }}                                                  \
              -C --coverage-basedir modules/lib/golioth-firmware-sdk                \
              --coverage-formats txt                                                \
              ${EXTRA_TWISTER_ARGS}                                                 \
              -x=CONFIG_GOLIOTH_COAP_HOST_URI=\"${{ inputs.coap_gateway_url }}\"    \
              --pytest-args="--api-url=${{ inputs.api-url }}"                       \
              --pytest-args="--api-key=${{ secrets[inputs.api-key-id] }}"           \
              --pytest-args="--mask-secrets"                                        \
              --pytest-args="--alluredir=allure-reports"                            \
              --pytest-args="--runner-name=${{ runner.name }}"                      \
              --pytest-args="--hil-board=${{ inputs.platform }}"

      - name: Safe upload twister artifacts
        if: success() || failure()
        uses: ./modules/lib/golioth-firmware-sdk/.github/actions/safe-upload-artifacts
        with:
          secrets-json: ${{ toJson(secrets) }}
          name: twister-run-artifacts-${{ inputs.platform }}-${{ matrix.name }}
          path: |
            reports/*
            twister-out/coverage.json
            twister-out/**/*.log
            twister-out/**/report.xml
            twister-out/*.xml
            twister-out/*.json

      - name: Safe upload CI report summary
        uses: ./modules/lib/golioth-firmware-sdk/.github/actions/safe-upload-artifacts
        if: success() || failure()
        with:
          name: ci-individual-samples-zephyr-${{ inputs.platform }}-${{ matrix.name }}
          path: twister-out/twister_suite_report.xml

      - name: Safe upload Allure reports
        if: success() || failure()
        uses: ./modules/lib/golioth-firmware-sdk/.github/actions/safe-upload-artifacts
        with:
          secrets-json: ${{ toJson(secrets) }}
          name: allure-reports-samples-zephyr-${{ inputs.platform }}-${{ matrix.name }}
          path: allure-reports

  verify_build_and_test:
    name: zephyr-${{ inputs.west_board }}-twister-verify-build-and-test
    if: always()
    needs:
      - matrix
      - build_and_test
    runs-on: ubuntu-24.04

    steps:
      - name: Decide whether the needed jobs succeeded or failed
        uses: golioth/alls-green@e956ef40847ee355e2e20264e479f0cacaaa2139
        with:
          jobs: ${{ toJSON(needs) }}
          action-summary-output: quiet-on-success

  hil_sample_zephyr_nsim_summary:
    name: zephyr-${{ inputs.platform }}-twister-summary
    runs-on: ubuntu-24.04
    needs:
      - matrix
      - build_and_test
    if: success() || failure()

    steps:
      - name: Collect JUnit reports
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: ci-individual-samples-zephyr-*

      - name: Prepare CI report summary
        if: success() || failure()
        run: |
          sudo apt install -y xml-twig-tools

          rm -rf summary
          mkdir summary

          xml_grep --pretty_print indented \
            --wrap testsuites --descr '' \
            --cond "testsuite" \
            ci-individual-samples-zephyr-${{ inputs.platform }}*/*.xml \
            > samples-zephyr-${{ inputs.platform }}.xml

      - name: Upload CI report summary
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: success() || failure()
        with:
          name: ci-summary-samples-zephyr-${{ inputs.platform }}
          path: samples-zephyr-*.xml
