manifest:
  self:
    path: modules/lib/golioth-firmware-sdk
    import:
      file: west-zephyr.yml
      name-allowlist:
        - zephyr
    userdata:
      patches_dirs:
        - patches/west-zephyr
